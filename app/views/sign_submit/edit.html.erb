<% content_for :card_title, "Type your name to sign this change report." %>

<% content_for :card_body do %>
    <p><%= t(self_or_other_member_translation_key(".submit_agreement"), member_names: current_report.member_names) %></p>

    <%= fields_for(:form, @form, builder: Cfa::Styleguide::CfaFormBuilder) do |f| %>
      <%= f.cfa_input_field :signature, "Type your full legal name" %>
    <% end %>
<% end %>

<% content_for :card_footer_button_label, "Sign and submit" %>
